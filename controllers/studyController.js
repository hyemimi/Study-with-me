const getUserStudies = async (req, res) => {
    // 특정 user의 스터디 리스트들을 불러옵니다
    try {

    } catch (error) {
        res.send("error.message");
    }
}

const addStudies = async (req, res) => {
    // 새로운 스터디를 생성합니다
    try {

    } catch (error) {
        res.send("error.message");
    }
}

module.exports= {getUserStudies, addStudies}